# Server-side proxy target (recommended for production/Vercel)
BACKEND_URL=http://localhost:8000

# Optional: direct browser-to-backend URL (requires backend CORS configuration)
NEXT_PUBLIC_API_URL=http://localhost:8000
